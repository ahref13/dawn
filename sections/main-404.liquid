{% comment %} THE FALL – 404 Page {% endcomment %}

<section class="flex flex-col items-center justify-center min-h-screen px-[20px] md:px-[30px] lg:px-[50px]">
  <div class="text-center">
    <h1 class="font-sm uppercase font-normal mb-4">Page not found</h1>
    <p class="font-sm mb-8">The page you are looking for does not exist.</p>
    <a href="{{ routes.root_url }}" class="font-sm uppercase underline">
      Return home
    </a>
  </div>
</section>

{% schema %}
{
  "name": "TF • 404",
  "settings": []
}
{% endschema %}
